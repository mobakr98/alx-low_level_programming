#include "main.h"
#include <unistd.h>

/**
 * mul - function that multiplies two integers
 *
 * @a: 1st int to mul
 * @b: 2nd int to mul
 *
 * Return: the result of multiplication
 *
 */
int mul(int a, int b)
{
int mul = a * b;

return (mul);
}
