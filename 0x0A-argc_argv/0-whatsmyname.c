#include <stdio.h>

/**
 * main - the main function
 *
 * @argc: arguments counter
 * @argv: arguments vector
 *
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
void(argc);

printf("%s\n", argv[0]);

return (0);
}
